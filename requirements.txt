pandas>=2.2,<3.0
requests>=2.32,<3.0
yfinance>=0.2.66,<0.3.0
- name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # <--- ESTO le dice a GitHub: "Mira el requirements.txt"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt # <--- USA el archivo para instalar
